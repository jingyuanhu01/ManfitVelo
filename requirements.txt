# Core dependencies for scripts/ (the method implementation) and simulation/
# (the P0-P5 experiment suite). Loose lower-bound pins -- known-working
# versions (2026-08-12) noted in comments; no known upper-bound constraints.
numpy>=2.1        # 2.1.3
pandas>=2.2        # 2.2.3
scikit-learn>=1.6  # 1.6.1
matplotlib>=3.10   # 3.10.0
scipy>=1.15         # 1.15.3
PyYAML>=6.0         # 6.0.2
pytest>=8.3         # 8.3.4 -- test suite: `pytest -q simulation`
